import { render, screen } from '@testing-library/react';
import ProductOverview from './ProductOverview';
/*
test('ProductOverview renders Carousel and Products', () => {
    const mockData = [
        { product_id: "1", highlighted: true, product: "Highlighted Product", price: 100 },
        { product_id: "2", highlighted: false, product: "Regular Product", price: 50 }
    ];
    render(<ProductOverview data={mockData} />);

    // Assuming your CarouselComponent conditionally renders based on `highlighted` data
    expect(screen.getByText('Highlighted Product')).toBeInTheDocument();
    expect(screen.getByText('Regular Product')).toBeInTheDocument();
});
*/
